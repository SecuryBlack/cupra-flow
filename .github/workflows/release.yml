name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    uses: securyblack/sb-agent-core/.github/workflows/release.yml@main
    with:
      binary-name: cupraflow
      extra-files: "config/config.toml"
      targets-json: |
        [
          {"target":"x86_64-pc-windows-msvc","os":"windows-latest","archive_ext":"zip","cross":false}
        ]
